export const AppData = ({ footerHeight }: propsType) => {
    return (
        <aside className="app-layout--app-data__aside-container">
        </aside>
    )
}


type propsType = {
    footerHeight: number
}